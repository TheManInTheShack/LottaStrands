---
date: 2026-03-31
---

# Session 2026-03-31

Continuation session. Focused on workflow/tooling setup, UI polish, file cleanup, and the opening animation.

## Done

**Workflow & documentation**
- Created `/wrap` slash command (`.claude/commands/wrap.md`) — updates status.md + session note and commits; triggered manually or proactively by Claude when context window is getting full
- Discussed context window mechanics with user; agreed Claude will flag when window is getting full and offer to wrap before continuing
- Committed and pushed the full documentation vault (CLAUDE.md, docs/) from prior session

**UI polish**
- New Volume form panel made fully opaque (`StyleBoxFlat` with `alpha=1`, solid dark background, 24px padding) — background bleed-through was distracting

**File cleanup (Godot)**
- Renamed `Main.gd/.tscn` → `CurationScreen.gd/.tscn` (that's what it actually is)
- Deleted dead scenes: `SceneCard`, `SceneList`, `SceneDetail` (superseded by CurationView architecture)
- Updated `CorpusMenu.gd` reference from `Main.tscn` → `CurationScreen.tscn`
- Added **Code hygiene** section to `CLAUDE.md` instructing future instances to raise cleanup passes proactively after significant change batches

**Opening animation**
- Created `PlotZIntro.gd/.tscn` — engine-level PLOT word sequence animation (Parse→Lexicon→Ontology→Taxonomy→Z→PLOTZ); each word fades in (0.4s), holds (0.75s), fades out (0.25s); PLOTZ holds 1.5s, fades 0.5s; chains to TitleCard
- Created `TitleCard.gd/.tscn` — corpus-specific title screen; "LOTTA STRANDS" + monospace subtitle; fades in (0.4s), holds (1.5s), fades out (0.25s); chains to CorpusMenu
- Split from original combined TitleCard at user's request: PlotZIntro is engine-reusable; TitleCard is per-repo
- Updated `project.godot` main scene to `PlotZIntro.tscn`
- Font for "LOTTA STRANDS" is placeholder (Impact italic); user to swap in psychedelic .ttf later; comment in tscn explains exactly how

**In progress at session end**
- Started work on `docs/howto/new-model-repo.md` — guide for spinning up a new corpus instance

## Decided
- PlotZIntro lives in the engine; TitleCard lives in the corpus repo — explicit engine/corpus boundary
- Code hygiene raises are proactive, not on request
- `/wrap` is the canonical end-of-session command; Claude calls it without being asked when context is low

## Next
- Write `docs/howto/new-model-repo.md` (in progress)
- Verify New Volume creation end-to-end on VM
- Test multi-volume append
- Test InsertStrip → scene split and HierarchyMarker remove → merge flows
- Delete `model/data/parsed.json` (dead artifact)
- Custom font for TitleCard when user is ready
- Godot web export
- DigitalOcean deployment
